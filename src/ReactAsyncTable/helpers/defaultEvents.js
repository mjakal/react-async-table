export const onChangePage = () => {
  throw Error('Add onChangePage(page) method to component props');
};

export const onSearch = () => {
  throw Error('Add onSearch(search) method to component props');
}

export const onSort = () => {
  throw Error('Add onSort(sortField, sortOrder) method to component props');
}

export const onInsert = () => {
  throw Error('Add onInsert() method to component props');
};

export const onEdit = () => {
  throw Error('Add onEdit(rowID) method to component props');
};

export const onDelete = () => {
  throw Error('Add onDelete(rowID, goToPage) method to component props');
};

export const onMultipleDelete = () => {
  throw Error('Add onMultipleDelete(values, goToPage) method to component props');
};

export const onAction = () => {
  throw Error('Add onAction(type, rowID) method to component props');
};

export const onColumnClick = () => {
  throw Error('Add onColumnClick() method to component props');
};
